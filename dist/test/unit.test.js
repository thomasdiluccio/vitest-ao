import { describe, it, expect } from 'vitest';
describe('vitest-ao', () => {
    it('should pass', () => {
        expect(1 + 1).toBe(2);
    });
    it('should fail', () => {
        expect(1 + 1).toBe(3);
    });
});
//# sourceMappingURL=unit.test.js.map